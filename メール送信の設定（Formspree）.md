# 予約が入ったらメールを受け取る設定（Formspree）

**目的**  
お客様が予約フォームから送信すると、あなたのメールアドレスに「予約が入りました」という内容が届くようにします。  
**Formspree**（フォームスプリー）という無料のサービスを使います。

---

## 全体の流れ（4つだけ）

1. Formspree に登録する  
2. 「フォーム」を1つ作る（ここに予約が届く受け皿を作る）  
3. 表示された **URL** をコピーする  
4. その URL を **config.js** に貼り付けて保存する  

これで終わりです。

---

## ステップ 1：Formspree に登録する

### 何をするか
Formspree のサイトで「このメールアドレスで使います」と登録します。

### やり方

1. ブラウザのアドレス欄に **`https://formspree.io`** と入力して Enter  
2. 画面が開いたら、右上にある **「Get Started」** または **「Sign Up」** をクリック  
3. **メールアドレス** を入力する  
   - ここには「予約の通知を受け取りたいアドレス」を入れます（例：`s.takumi19970328@gmail.com`）  
4. **「Sign up」** をクリック  
5. そのメールアドレスに Formspree からメールが届くので、**メールの中のリンクを1回クリック**して認証する  

→ これで「Formspree に登録した」状態になります。

---

## ステップ 2：フォームを1つ作る

### 何をするか
「予約が届く受け皿」を1つ作ります。Formspree ではこれを「フォーム」と呼びます。

### やり方

1. Formspree にログインした状態で、画面の **「New Form」**（新規フォーム）をクリック  

2. 次の2つを入力します  

   | 入力する場所 | 何を入れるか |
   |--------------|--------------|
   | **Form name**（フォーム名） | 自分が分かればよい名前。例：`ピラティス予約` |
   | **Email**（メール） | **予約の通知を届けたいメールアドレス**（例：`s.takumi19970328@gmail.com`） |

3. **「Create Form」** をクリック  

→ フォームが1つできました。次のステップで「このフォームの住所（URL）」をコピーします。

---

## ステップ 3：URL をコピーする

### 何をするか
「このフォームにデータを送るための住所」＝ **URL** をコピーします。この URL をサイト側（config.js）に貼り付けると、予約送信と同時に Formspree にデータが届き、メールが送られます。

### やり方

1. さきほど作ったフォームの画面が開いているはずです  
2. 画面上で **「Integration」** または **「Code」** というタブ（ボタン）をクリック  
3. その中に **「Form endpoint」** や **「Your form's endpoint」** と書いてあるところを探す  
4. その下に **長い URL** が表示されています  
   - 形はだいたい **`https://formspree.io/f/xxxxxxxx`** です（`xxxxxxxx` の部分は英数字）  
5. その URL を **マウスで選択してコピー**（右クリック → コピー、または Ctrl+C）  

→ この URL を次のステップで config.js に貼り付けます。

---

## ステップ 4：config.js に URL を貼り付ける

### 何をするか
プロジェクトの **config.js** というファイルを開き、コピーした URL を「決まった場所」に貼り付けて保存します。そうすると、予約送信のときに Formspree にデータが送られ、メールが届くようになります。

### やり方

1. プロジェクトのフォルダ（simuta）を開き、**config.js** をエディタで開く  

2. 中身のどこかに、次のような行があります  

   ```  
   var FORMSPREE_ENDPOINT = '';  
   ```  

3. **`''` のあいだ**（シングルクォート2つの間）に、さきほどコピーした URL を貼り付ける  

   **貼り付け前：**  
   ```  
   var FORMSPREE_ENDPOINT = '';  
   ```  

   **貼り付け後（例）：**  
   ```  
   var FORMSPREE_ENDPOINT = 'https://formspree.io/f/xxxxxxxx';  
   ```  

   - 前の **`'`** と、後ろの **`'`** は**消さずに残す**  
   - URL の前後に**スペースを入れない**  

4. **ファイルを保存**する（Ctrl+S など）  

→ これで「メールが届く設定」は完了です。

---

## 動作確認のしかた

1. 予約ページを **http://localhost:3333/booking.html** または **Vercel の URL** で開く（ファイルを直接開く `file://` では届きません）  
2. クラス・日付・時間・名前・電話・メールを入れて **「予約を送信する」** を押す  
3. 「予約リクエストを受け付けました」と出れば送信は成功  
4. 数分以内に、Formspree の **Email** に指定したアドレスにメールが届く（届かない場合は**迷惑メール**も確認）  

---

## 用語の説明

| 言葉 | 意味 |
|------|------|
| Formspree | フォームの送信を受け取って、指定したメールアドレスに内容を送ってくれる無料サービス |
| フォーム | ここでは「予約が届く受け皿」のこと。Formspree で1つ作る |
| URL | フォームの「住所」。`https://formspree.io/f/xxxxxxxx` のような文字列 |
| config.js | このサイトの設定を書いておくファイル。URL やキーをここに書く |
| FORMSPREE_ENDPOINT | 「Formspree のフォームの URL をここに書く」という設定の名前 |

---

## メールが届かないとき

- 予約ページは **file://** ではなく **http://localhost:...** または **https://simuta.vercel.app/...** で開く  
- **config.js** の URL が、コピーしたものと**完全に同じ**か確認（余計なスペースがないか）  
- 保存したあと、ブラウザを**再読み込み**してからもう一度送信する  
- **迷惑メール（スパム）** フォルダを見る  
- Formspree の画面で **「Submissions」** を開き、送信が並んでいれば Formspree には届いているので、メール側（届き先・迷惑メール）を確認する  

---

## 注意

- **FORMSPREE_ENDPOINT** を空（`''`）のままにすると、メールは送られません（予約の保存だけされます）  
- 無料プランでは 1か月あたり **50通まで**  
- config.js は Git に含めない設定なので、この URL を GitHub に上げても漏れません  
